kind: Deployment
apiVersion: apps/v1
metadata:
  name: cart-deploy
  namespace: e-commerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart
  template:
    metadata:
      labels:
        app: cart
    spec:
      containers:
        - name: cart
          image: nginx:latest
          ports:
            - containerPort: 80
          command: ["/bin/sh"]
          args:
          - -c
          - |
            mkdir -p /usr/share/nginx/html/cart
            cat > /usr/share/nginx/html/cart/index.html <<EOF
            <!DOCTYPE html>
            <html>
              <body>
                <h1>Welcome to Cart Service</h1>
                <h2>AWSome AI</h2>
              </body>
            </html>
            EOF
            nginx -g 'daemon off;'

---

apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: e-commerce
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /cart/index.html
spec:
  selector:
    app: cart
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
